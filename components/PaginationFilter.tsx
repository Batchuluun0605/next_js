import { Pagination } from "react-bootstrap";

const PaginationFilter = () => {
  return (
    <Pagination>
      <Pagination.Prev />
      <Pagination.Next />
    </Pagination>
  );
};

export default PaginationFilter;
